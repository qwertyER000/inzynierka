Biblioteki potrzebne: ColorMsappig, CameraCV - w folderze GitHub files

W ImageMapping.cpp:
1. Zaimplementowany Solver, ale kiedy results.size<params.size to nie uruchamia nawet solvera. Żeby działało jako results wyrzucam wektor w którym wszystkie pola są równe countNonZeros/all_pixels, ale solver w ogóle nie rusza tego, nic nie zmienia ten optimizer.
TO DO:
a) optymalizacja
b) ostateczne spięcie tego aby zrównoważyć dokładność i czas wykonywania dopasowania
c) projekcja
